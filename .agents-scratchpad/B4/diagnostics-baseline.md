# B4 diagnostics baseline (pre-edit)

- Date: 2026-09-12
- Exclusive files: `dash/dash/src/App.tsx`, `dash/dash/src/pages/CompareRuns.tsx`, `dash/dash/src/components/kyber/TurnAlignedDiff.tsx`
- IDE `ReadLints` on those three files: no linter errors found
- ESLint (`dash/` cwd, project `npm run lint`): see `.agents-scratchpad/B4/lint-baseline.txt` — files under `dash/dash/` are in eslint ignore `dash/**` at this config, so the command is a no-op match / ignore — recorded anyway

No pre-existing diagnostics in task-scoped files.
