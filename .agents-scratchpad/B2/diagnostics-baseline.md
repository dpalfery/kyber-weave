# B2 diagnostics baseline (pre-edit)

- Date: 2026-09-12
- Exclusive files: `dash/dash/src/pages/Attention.tsx`, `dash/dash/src/components/kyber/index.ts` (ScorecardMatrix.tsx not yet created)
- IDE `ReadLints` on Attention.tsx and index.ts: no linter errors found
- ESLint (`dash/` cwd): see `dash/.agents-scratchpad/B2/lint-baseline.txt` (eslint_exit 0; files under `dash/dash/` are in eslint ignore `dash/**` at this config, so the command is a no-op match / ignore — recorded anyway)

No pre-existing diagnostics in task-scoped files.
