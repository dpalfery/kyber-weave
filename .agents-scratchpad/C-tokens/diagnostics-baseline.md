# C-tokens diagnostics baseline (pre-edit)

- Date: 2026-09-12
- Exclusive files: `dash/dash/src/index.css`
- IDE `ReadLints` on `dash/dash/src/index.css`: no linter errors found
- ESLint (`dash/` cwd, `./node_modules/.bin/eslint --no-error-on-unmatched-pattern dash/src/index.css`): see `lint-baseline.txt` (eslint_exit 0; file under `dash/dash/` is in eslint ignore `dash/**` at this config, so the command is a no-op match / ignore — recorded anyway)

No pre-existing diagnostics in the task-scoped file.
