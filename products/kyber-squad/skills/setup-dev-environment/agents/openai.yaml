interface:
  display_name: "Setup Dev Environment"
  short_description: "Bootstrap the host repository's development machines."
  default_prompt: "Use $setup-dev-environment to set up my development environment on this machine."
policy:
  allow_implicit_invocation: true
